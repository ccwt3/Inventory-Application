<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>information</title>
</head>
<body>
  <div class="flower__information">
    <h1><%= flower.seed %></h1>
    <p>Ideal climate: <%= flower.climate %></p>
    <p>Final size: <%= flower.size %></p>
    <p>Watering: <%= flower.consistency %></p>
    <p>Sunlight needed: <%= flower.sunlight %></p>
  </div>

  <div class="flower__buttons">
    <form action="/inventory/info?id=<%= flower.id %>&action=edit" method="post" class="edit">
      <button type="submit">Edit</button>
    </form>

    <form action="/inventory/info?id=<%= flower.id %>&action=delete" method="post">
      <button type="submit">Delete</button>
    </form>
  </div>
</body>
</html>